# Backend API Configuration (Server-side only - NEVER exposed to client)
# This is the external image recognition API endpoint
IMAGE_API_URL=http://192.168.1.233:5001/search

# API Key for authenticating requests to the external image API
IMAGE_API_KEY=your-secret-api-key-here

# CORS Configuration (Server-side only)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
